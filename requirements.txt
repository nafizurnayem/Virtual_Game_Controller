# Tested baseline for the Windows 10/11 x64 build.
# Keep these pinned so a future dependency release cannot silently break the EXE.
mediapipe==0.10.21
opencv-python==4.10.0.84
pynput==1.7.7
numpy==1.26.4
